import './bootstrap';

// Import Bootstrap's JS
import 'bootstrap';

// Import and start Alpine.js for reactive components used in blade views
import Alpine from 'alpinejs';
window.Alpine = Alpine;
Alpine.start();
